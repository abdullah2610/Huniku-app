{
  "name": "huniku-app",
  "private": true,
  "scripts": {
    "build": "cd web && npm install --include=dev --legacy-peer-deps && npm run build",
    "start": "node web/build/server/index.js"
  }
}
